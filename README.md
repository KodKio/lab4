# Проект для лабораторной 4

Пока что это заготовка проекта, которая выводит hello world

Запуск:

* `python lab4.py`

Новые функции:

* `good_function()` возвращает Hi
* `bad_function()` возвращает 0